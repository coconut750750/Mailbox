<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar"><!--Theme.AppCompat.Light.DarkActionBar-->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--item name="android:windowTranslucentStatus">true</item-->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- progress dialog -->
    <style name="AppTheme.Dark.Dialog" parent="Theme.AppCompat.Dialog">
        <item name="colorAccent">@color/favoriteAccent</item>
        <item name="android:textColorPrimary">@color/favoriteLighter</item>
        <item name="android:background">@color/colorLogin</item>
    </style>

    <!-- fonts -->
    <style name="ralewayLight" parent="android:TextAppearance">
        <!-- Custom Attr-->
        <item name="fontPath">fonts/Raleway-Light.ttf</item>
    </style>

    <style name="ralewayReg" parent="android:TextAppearance">
        <!-- Custom Attr-->
        <item name="fontPath">fonts/Raleway-Regular.ttf</item>
    </style>

</resources>
